#include <iostream> 
 #include <cstdlib> 
 using namespace std; 
 main() 
 { 
 int peras, frutas; 
  system("CLS"); 
  cout << "Digite o numero de peras que estao dentro da cesta: "; 
  cin >> peras; 
  cout << "peras = " << peras; 
  cout << "\n ++peras = " <<++peras; 
  cout <<"\n--peras = "<< --peras; 
  cout << "\nperas ++ = "<<peras++; 
  cout << "\nperas -- ="<<peras--; 
  cout << "\nperas = " <<peras;
 }
